import 'package:flutter/material.dart';class TimetableThumbnail extends StatelessWidget{final String title;const TimetableThumbnail({super.key,required this.title});@override Widget build(c){return Container(width:double.infinity,padding:const EdgeInsets.all(24),decoration:BoxDecoration(color:Colors.blueGrey.shade100,borderRadius:BorderRadius.circular(16)),child:Text(title,style:const TextStyle(fontSize:24,fontWeight:FontWeight.bold)));}}